#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
#define rrep(i,a,b) for(int i = a; i >= b; i--)
using ll = long long;

int main(){
    return 0;
}

/*
文字は'', 文字列は""で括る
全角文字は文字列扱いなので, ""で括る

２次元配列
vector<vector<要素の型>> 変数名(縦の要素数, vector<要素の型>(横の要素数));


3次元配列
vector<vector<vector<要素の数>>> 変数名(要素数1, vector <vector<要素の型>>(要素数2, vector<要素の型>(要素数3,初期値)));

vector<
  vector
<vector
<要素の型>>  2次元配列の型
> 変数名(要素数1,
  vector
<vector
<要素の型>>(要素数2, vector
<要素の型>(要素数3, 初期値))  2次元配列の値
);
*/
