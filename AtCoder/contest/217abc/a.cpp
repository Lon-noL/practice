#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
#define rrep(i,a,b) for(int i = a; i >= b; i--)
using ll = long long;

int main(){
    string S, T;
    cin >> S >> T;

    if (S < T) cout << "Yes" << endl;
    else cout << "No" << endl;
    return 0;
}
